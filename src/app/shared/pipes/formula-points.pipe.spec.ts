import { FormulaPointsPipe } from './formula-points.pipe';

describe('FormulaPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormulaPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
