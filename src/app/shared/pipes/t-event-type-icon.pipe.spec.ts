import { TEventTypeIconPipe } from './t-event-type-icon.pipe';

describe('TEventTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TEventTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
